package DataAbstractionwithInterface;

public class PackagesAccessModifiers {
	
	
	/*  Packages and Access Modifiers
	 * 
	 *   1) Built in packages
	 *   2) User defined packages
	 *   
	 *   util is the ex of Built in packages. Built in packages contains Build in class and methods
	 *   
	 *   we need to import the package 
	 *   
	 *   
	 *   
	 *   Access Modifiers 
	 *   
	 *   Public  - Can access anywhere just create the object
	 *   Protected - 
	 *   Default - Access only with in the package
	 *   Private - Access only with in the class
	 * 
	 *   If we want to access the another class (class2) (From the different package (package 2)) from your class (Class 1)
	 *   need to import that packege 
	 *   
	 *   import.pacakge2.class2
	 *   
	 * 
	 */

}
